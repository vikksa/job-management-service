spring:
  datasource:
    url: jdbc:mysql://db:3306/job-service
    username: root
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
  flyway:
    enabled: true

smtp:
  hostname: smtp.sendgrid.net
  port: 587
  from: test@gmail.com
  authentication:
    username: apikey
    password:
